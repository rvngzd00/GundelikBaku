services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: dailybaku
      POSTGRES_USER: dailybaku
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD tələb olunur}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dailybaku -d dailybaku"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks: [private]

  app:
    build: .
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
      PUBLIC_ORIGIN: ${PUBLIC_ORIGIN:?PUBLIC_ORIGIN tələb olunur}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-}
      DATABASE_URL: postgresql://dailybaku:${POSTGRES_PASSWORD}@postgres:5432/dailybaku
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET tələb olunur}
      COOKIE_SECRET: ${COOKIE_SECRET:?COOKIE_SECRET tələb olunur}
      BOOTSTRAP_ADMIN_EMAIL: ${BOOTSTRAP_ADMIN_EMAIL:-admin@gundelikbaki.az}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:?BOOTSTRAP_ADMIN_PASSWORD tələb olunur}
      EMAIL_PROVIDER: ${EMAIL_PROVIDER:-resend}
      RESEND_API_KEY: ${RESEND_API_KEY:?RESEND_API_KEY tələb olunur}
      EMAIL_FROM: ${EMAIL_FROM:-Gündəlik Bakı <noreply@gundelikbaki.az>}
      EMAIL_REPLY_TO: ${EMAIL_REPLY_TO:-support@gundelikbaki.az}
      EMAIL_TIMEOUT_MS: ${EMAIL_TIMEOUT_MS:-10000}
      TRUST_PROXY: "true"
      UPLOAD_DIR: /app/backend/uploads
    volumes:
      - uploads:/app/backend/uploads
    networks: [private, public]

  caddy:
    image: caddy:2.10-alpine
    restart: unless-stopped
    depends_on: [app]
    environment:
      DOMAIN: ${DOMAIN:?DOMAIN tələb olunur}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./docker/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks: [public]

networks:
  private:
    internal: true
  public:

volumes:
  postgres_data:
  uploads:
  caddy_data:
  caddy_config:
